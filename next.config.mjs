/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['github.com']
        //domains: ['images.unsplash.com', 'res.cloudinary.com']
    }
};

export default nextConfig;
